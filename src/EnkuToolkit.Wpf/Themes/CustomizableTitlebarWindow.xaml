<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:controls="clr-namespace:EnkuToolkit.Wpf.Controls"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="controls:CustomizableTitlebarWindow">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:CustomizableTitlebarWindow">
                    <DockPanel LastChildFill="True" Background="{TemplateBinding Background}">
                        <ContentControl Name="titlebar" DockPanel.Dock="Top" />
                        <ContentPresenter DockPanel.Dock="Bottom" />
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>