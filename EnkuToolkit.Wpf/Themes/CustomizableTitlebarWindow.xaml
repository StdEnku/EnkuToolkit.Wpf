<!--
    These codes are licensed under CC0.
    https://creativecommons.org/publicdomain/zero/1.0/deed 
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:controls="clr-namespace:EnkuToolkit.Wpf.Controls"
                    xmlns:me="clr-namespace:EnkuToolkit.Wpf.MarkupExtensions"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="controls:CustomizableTitlebarWindow" BasedOn="{me:GetApplicationResource {x:Type Window}}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:CustomizableTitlebarWindow">
                    <DockPanel LastChildFill="True" Background="{TemplateBinding Background}">
                        <ContentControl Name="titlebar" DockPanel.Dock="Top" />
                        <ContentPresenter DockPanel.Dock="Bottom" />
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>